# 上线前改密码：kubectl -n nexus create secret generic nexus-postgres --from-literal=... --dry-run=client -o yaml
apiVersion: v1
kind: Secret
metadata:
  name: nexus-postgres
  namespace: nexus
type: Opaque
stringData:
  POSTGRES_DB: nexus
  POSTGRES_USER: nexus
  POSTGRES_PASSWORD: nexus_change_me
